import { Container } from '../Container';
import { Socials } from '../Socials';
import { StyleFooter } from './Footer.styles';

export const Footer = () => {
  return (
    <StyleFooter>
      <Container>
        <Socials></Socials>
      </Container>
    </StyleFooter>
  );
};
